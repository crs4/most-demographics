language: python
python:
  - "2.7"
env:
  - DJANGO=1.6.2
install:
  - pip install -q Django==$DJANGO --use-mirrors
before_script:
  - cd examples/most/
script:
  - PYTHONPATH=.. python manage.py test most.web.demographics
